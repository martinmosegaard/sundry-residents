int isPrime(unsigned long long n);

int isPrimeInt(int n);

// Find the first prime factor of n or 0 if there is none.
int primeFactor(int n);

// Count the number of distinct prime factors of n.
int primeFactorCount(int n);

// Euler's totient function.
// phi(n) is the number of numbers less than n that are coprime / relatively prime to n
int phi(int n);
